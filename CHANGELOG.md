# บันทึกการเปลี่ยนแปลง — พร้อมเบิก (Promberk)

ดูเวอร์ชันที่ระบบกำลังใช้ได้ที่ **แถบบนสุดของทุกหน้า** และ **มุมล่างหน้าเข้าสู่ระบบ**
ป้ายจะบอกว่าเป็น `UAT` (ทดสอบ) หรือ `PROD` (ระบบจริง) พร้อมเลขเวอร์ชัน

ถ้าเลขสองฝั่งไม่ตรงกัน = ยังไม่ได้ deploy ของใหม่ขึ้น production

---

## 2.9.4 — 22 ก.ย. 2569

**ขากลับขึ้น 0 และคำเตือนเก่าค้างอยู่**

ลบชื่อสถานที่ในช่องขากลับออกแล้ว แต่ระยะทางยังค้างที่ 0 และคำเตือน "หาตำแหน่งไม่เจอ"
ยังอยู่ ทั้งที่ช่องว่างแล้ว

ต้นเหตุ: `syncReturnDiff()` เปลี่ยนความหมายของเส้นทาง (จาก "ไปคนละที่" เป็น "กลับทางเดิม")
**แต่ไม่ได้สั่งคำนวณใหม่** ตัวเลขกับคำเตือนเดิมจึงค้างบนจอ

- ลบข้อความแล้วคิดระยะทางใหม่ทันที
- **หมุดของช่องที่ถูกลบข้อความ ถูกเอาออกด้วย** ไม่งั้นระบบยังคิดจากหมุดเก่าที่ผู้ใช้เอาออกไปแล้ว
- คำเตือนที่หมดอายุถูกล้าง ไม่ค้างให้เข้าใจผิด

**เรื่องเส้นขากลับบนแผนที่** — ไม่ได้หาย แต่กลับทางเดิมจึงทับเส้นเดิมพอดี วาดซ้ำก็ไม่เห็นอะไรเพิ่ม
เพิ่มป้ายบอกแทนว่า `ขากลับใช้เส้นทางเดิม 42.2 กม.` จะได้ไม่ดูเหมือนระบบลืมขากลับ
ถ้าขากลับไปคนละทางจริง เส้นสีม่วงประจะถูกวาดพร้อมหมุดของตัวเองอยู่แล้ว **แผนที่เดียวจึงพอ**

**หน้าประวัติ: ชื่อกิจกรรมยาวดันปุ่มออกนอกจอ**

ตารางตั้ง `whitespace-nowrap` ไว้ ความกว้างจึงยืดตามชื่อที่ยาวที่สุด
ต้องเลื่อนไปทางขวาไกลมากกว่าจะกดปุ่ม PDF ได้

- ชื่อกิจกรรมและชื่อผู้ขอเบิกตัดด้วย `...` ข้อความเต็มดูได้จากการชี้เมาส์
- กำหนดความกว้างแต่ละคอลัมน์ ตารางจึงพอดีจอ **ไม่ต้องเลื่อนแนวนอนเลย**
- **ตรึงคอลัมน์ปุ่มไว้ขอบขวา** เผื่อจอแคบจนยังต้องเลื่อน — ทดสอบที่ 760px แล้วยังกดได้

**ทดสอบ 47 ข้อในชุดการ์ด และเพิ่มชุดใหม่สำหรับตารางหน้าประวัติ 8 ข้อ**
ชุดอื่นอีก 51 ข้อยังผ่านครบ

---

## 2.9.3 — 22 ก.ย. 2569

**ฟอร์มบอกอัตราค่าน้ำมันผิด ผู้ใช้เลยคิดตามแล้วไม่ตรง**

ผู้ใช้แจ้งว่าระยะทาง 23.4 + 23.4 กม. อัตรา 4 บาท ควรได้ 187.20 แต่ระบบขึ้น 234.00

ตรวจแล้ว **ยอด 234 ถูกต้อง** เพราะ 46.8 × **5** = 234
อัตรากลางในระบบคือ 4 จริง แต่ **หมวดหมู่ตั้งทับได้** และ 3 ใน 4 หมวดตั้งไว้ที่ 5

**ที่ผิดคือฟอร์ม ไม่ใช่การคำนวณ** — ตอนออกแบบการ์ดต่อขา ผมซ่อนช่อง `fuelRate`
แล้วเขียนเลข **4** ตายตัวลงในคำอธิบาย ผู้ใช้จึงเห็น 4 ขณะที่ระบบคิดด้วย 5

- ตัวเลขอัตราอ่านจากค่าจริงแล้ว เปลี่ยนหมวดก็เปลี่ยนตาม
- **เพิ่มบรรทัดกางวิธีคิด** ให้ตรวจตามด้วยเครื่องคิดเลขได้

  `วิธีคิดค่าน้ำมัน: ขาไป 23.4 กม. + ขากลับ 23.4 กม. = 46.8 กม. × 5 บาท = 234.00 บาท`

ยอดค่าน้ำมันเป็นเลขที่ผู้ใช้คำนวณเองในใจได้ ถ้าตัวเลขบนจอไม่ตรงกับที่เขาคิด
ต้องเห็นได้ทันทีว่าต่างกันตรงไหน ไม่ใช่ต้องมาถาม

**ทดสอบเพิ่ม 4 ข้อ รวมเป็น 43 ข้อ** ชุดอื่นอีก 51 ข้อยังผ่านครบ

---

## 2.9.2 — 22 ก.ย. 2569

**Taxi ไม่ต้องกรอกวันที่ซ้ำ และติ๊กแค่ Taxi ก็ต้องปักหมุดได้**

- **วันที่ของแถว Taxi เติมให้เองจากวันของขานั้น** การ์ดบอกอยู่แล้วว่าขาไปคือวันไหน
  ไม่มีเหตุผลให้กรอกซ้ำ เปลี่ยนวันเดินทางด้านบน แถวที่ยังไม่เคยแก้เองจะตามไปด้วย
  ส่วนแถวที่ผู้ใช้แก้วันเองไว้แล้ว จะไม่ถูกเขียนทับ

- **แก้ที่ผมทำพังเองตอนออกแบบการ์ด** — ช่องจุดเริ่มต้น/จุดหมายไปอยู่ใต้ติ๊ก "รถส่วนตัว"
  แต่วงเงินค่า Taxi ในเขต กทม.ปริมณฑล (ไม่เกิน 600 บาท) คิดจากหมุดสองจุดนี้
  ติ๊กแค่ Taxi จึงปักหมุดไม่ได้ และ**วงเงินไม่ทำงาน**
  ย้ายออกมาเป็น "เส้นทางของขาไป" ที่ใช้ร่วมกัน โผล่เมื่อเบิกอะไรก็ตามในขานั้น

**เรื่องระยะทางของ Taxi** — เอกสารต้นแบบ FM-SAM-095-00 มีแค่ จาก / ถึง / จำนวนเงิน
ไม่มีช่องระยะทางเหมือนค่าน้ำมัน เพราะ Taxi เบิกตามค่าโดยสารจริง ไม่ได้คิดจากระยะทาง × อัตรา
จึงไม่ได้เพิ่มช่องระยะทางให้ แต่หมุดบนแผนที่ยังจำเป็นสำหรับตรวจวงเงิน

**ทดสอบเพิ่ม 8 ข้อ รวมเป็น 39 ข้อ** ชุดอื่นอีก 51 ข้อยังผ่านครบ

---

## 2.9.1 — 22 ก.ย. 2569

**บ้านที่แผนที่หาไม่เจอ ต้องไม่ทำให้เบิกขากลับไม่ได้**

พอแยกขากลับออกมาเป็นช่องของตัวเอง ก็เจอว่าบ้านเลขที่ในซอยมักค้นไม่เจอในแผนที่
ขึ้นแดงว่า "หาตำแหน่งของ ... ไม่เจอ" แล้วไปต่อไม่ได้

แก้สามจุดที่ซ้อนกันอยู่

- **ช่องเขียนว่า "ว่าง = กลับที่จุดเริ่มต้น" แต่โค้ดไม่ได้ทำตามนั้น** เว้นว่างแล้วได้ระยะทาง **0**
  ตอนนี้เว้นว่างแล้วใช้พิกัดจุดเริ่มต้นขาไปจริง ๆ ซึ่งเป็นบ้านที่ดึงมาจากโปรไฟล์และมีพิกัดอยู่แล้ว
  **จึงไม่ต้องพิมพ์ที่อยู่บ้านให้แผนที่หาเลย**
- **คำเตือนของช่อง "จุดเริ่มต้นขากลับ" ไปโผล่ใต้ช่อง "ปลายทางขากลับ"** อ่านแล้วงงว่าช่องไหนผิด
  ตอนนี้แต่ละช่องมีที่แสดงคำเตือนของตัวเอง
- **ข้อความตอนหาไม่เจอ ไม่ได้บอกทางออกที่ง่ายที่สุด** เดิมบอกให้ลากหมุดหรือกรอกระยะทางเอง
  ตอนนี้บอกก่อนเลยว่า ถ้าจะกลับบ้าน ให้ลบข้อความในช่องออก ระบบใช้พิกัดที่มีอยู่แล้วให้เอง

และช่องขากลับบอกชื่อสถานที่จริงใน placeholder เช่น `ว่าง = กลับที่ 141/36 ซอยสุขสวัสดิ์ 55`
แทนข้อความลอย ๆ ผู้ใช้จะเห็นทันทีว่าเว้นว่างไว้ได้เลย ไม่ต้องไปพิมพ์ที่อยู่บ้าน

**ทดสอบเพิ่ม 6 ข้อ รวมเป็น 31 ข้อ** — ข้อสำคัญที่สุดคือเว้นช่องปลายทางขากลับว่างแล้ว
ระบบคำนวณ รพ.กรุงเทพ → บ้าน ได้ 42 กม. โดยใช้พิกัดบ้านที่รู้อยู่แล้ว ไม่ใช่ 0 เหมือนเดิม
ชุดอื่นอีก 51 ข้อยังผ่านครบ

---

## 2.9.0 — 22 ก.ย. 2569

**เลือกการเดินทางทีละขา — รองรับเคสผสมและกันเบิกเกินโดยไม่ตั้งใจ**

เดิมถามแยกกันคนละที่ คือ "รายการ Taxi" กล่องหนึ่ง กับ "รูปแบบรถส่วนตัว" อีกกล่อง
คนกรอกต้องประกอบภาพเองว่าขาไหนใช้อะไร

**ปัญหาที่หนักกว่านั้น** — ช่องรถส่วนตัวตั้งค่าเริ่มต้นไว้ที่ `ไป-กลับ` และแผนที่เติมระยะทางให้อัตโนมัติ
คนที่นั่ง Taxi ล้วนแล้วไม่ได้ไปแตะช่องนั้น **จะถูกคิดค่าน้ำมันที่ไม่ได้จ่าย**
ทดสอบแล้วเกิดขึ้นจริง: ยอดขึ้นมา 800 บาทเองทั้งที่ยังไม่ได้กรอกอะไรเลย

ตอนนี้เปลี่ยนเป็นการ์ดต่อขา

```
ขาไป   20 ต.ค. 2568      ขากลับ  22 ต.ค. 2568
 ☑ รถส่วนตัว              ☐ รถส่วนตัว
   จุดเริ่มต้น / จุดหมาย     ☑ Taxi
 ☐ Taxi                     วันที่ / จาก / ถึง / ค่าโดยสาร
```

- **ไม่ติ๊ก = ไม่คิดเงิน** ค่าเริ่มต้นปลอดภัยแทนที่จะเดาให้
- **ขาเดียวติ๊กได้หลายอย่าง** เช่น ขาไปนั่ง Taxi ไปสนามบินแล้วต่อรถส่วนตัว
- ติ๊ก Taxi แล้วมีแถวให้กรอกทันที และ**ทิศทางถูกล็อกตามการ์ด** ไม่ต้องเลือกซ้ำ
- หัวการ์ดบอกวันที่ของขานั้น ไม่ต้องเลื่อนขึ้นไปดู
- "ขากลับไปคนละที่" ไม่ต้องติ๊กเองแล้ว ระบบดูจากช่องที่กรอก — เว้นว่าง = กลับทางเดิม

**ไม่แตะโครงข้อมูลเลย** ช่อง `carType` กับ `returnDiff` เดิมยังอยู่ แค่ซ่อนไว้แล้วให้การ์ดเป็นคนตั้งค่าให้
โค้ดคำนวณเงิน บันทึก และออกเอกสารจึงไม่ต้องแก้ และใบเก่าเปิดมาแก้ได้เหมือนเดิม
รายการ Taxi แบบ "ไป-กลับ" ของใบเก่าที่คร่อมสองขา วางไว้ที่การ์ดขาไปและเปิดช่องทิศทางให้เห็น

**ทดสอบ 25 ข้อเฉพาะเรื่องนี้** — ค่าเริ่มต้นไม่คิดเงินให้เอง · ติ๊กทีละขาแล้วยอดขยับตามจริง ·
เคสผสมขาไปรถส่วนตัว-ขากลับ Taxi ได้ยอด 850 ถูกต้อง · เอาติ๊กออกแล้วไม่มียอดค้าง ·
ขากลับคนละที่ระบบรู้เอง · หัวการ์ดขึ้นวันที่ถูก  ชุดเดิมอีก 40 ข้อยังผ่านครบ

**ตรวจเคสผสมกับเอกสารจริง 5 แบบ** ยอดถูกทุกแบบ — ขาไปรถ/ขากลับ Taxi 850 ·
ขาไป Taxi/ขากลับรถ 800 · รถล้วน 800 · Taxi ล้วน 850 · Taxi 2 เที่ยวคนละวัน + รถขาไป 1,020

---

## 2.8.1 — 22 ก.ย. 2569

**"คำนวณไม่สำเร็จ: HTTP 502" ตอนลากหมุด**

ไล่จาก log แล้วพบว่าคำขอแรกไป Longdo **ค้างจน nginx ตัดที่ 30 วินาที (504)**
คำขอถัดมาได้ 502 ที่ออกจากโค้ดเราเอง ตรวจซ้ำภายหลังพบว่า Longdo ตอบปกติ
(83–274 มิลลิวินาที ยิงพร้อมกัน 6 คำขอก็ผ่านหมด) จึงเป็น**อาการสะดุดชั่วคราวของผู้ให้บริการ**

ทำให้ทนการสะดุดได้แทนที่จะปล่อยให้ผู้ใช้เจอเต็มๆ

- **ตัดเวลาที่ 8 วินาที** เดิมไม่มีกำหนดเวลาเลย คำขอจึงค้างยาวจน nginx ตัด
- **ลองซ้ำอีกครั้ง** สะดุดครั้งเดียวจะไม่ถึงมือผู้ใช้
- **บันทึกสาเหตุจริงลง log** เดิม catch กลืน error ทิ้งโดยไม่บันทึกอะไร พอเกิดจริงจึงไล่ย้อนหลังไม่ได้
- **ข้อความที่คนอ่านรู้เรื่อง** เดิมขึ้นว่า "HTTP 502" ซึ่งไม่ได้บอกอะไรกับคนกรอกฟอร์ม
  ตอนนี้บอกว่าเป็นอาการชั่วคราวของผู้ให้บริการ ไม่ใช่ที่หมุด พร้อมทางออกว่ากดใหม่หรือกรอกเอง

ย้ายตัวเรียกไปไว้ที่ `backend/longdo-fetch.js` เพื่อให้ชุดทดสอบเรียกของจริงตัวเดียวกับที่ระบบใช้
ไม่ใช่คัดลอกโค้ดไปไว้อีกที่แล้วเพี้ยนจากกันภายหลัง

**ทดสอบ 11 ข้อ** — ปลายทางปกติยิงครั้งเดียว · ปลายทางค้างต้องตัดเวลาและไม่เงียบหาย ·
ปลายทางตอบผิดพลาดต้องแยกออกจากการหมดเวลาได้ · สะดุดครั้งเดียวต้องกู้คืนเองโดยผู้ใช้ไม่เห็น
และชุดเดิมอีก 40 ข้อยังผ่านครบ

---

## 2.8.0 — 22 ก.ย. 2569

**ใบรับรองแทนใบเสร็จรับเงิน - ค่าพาหนะ ล้อตามเอกสารต้นแบบ FM-SAM-095-00**

เดิมวันที่เดียวลอยอยู่บรรทัดบนสุด แล้วกินยาวคร่อมทั้งตาราง 16 แถว
อ่านไม่ออกว่าวันไหนเป็นของขาไป วันไหนเป็นของขากลับ

ตอนนี้วันที่อยู่บรรทัดเดียวกับขาเดินทางนั้น เหมือนในเอกสารต้นแบบ

| บรรทัด | วันที่ที่ขึ้น |
|---|---|
| Taxi แต่ละรายการ | วันที่ของรายการนั้น (ฟอร์มเก็บไว้อยู่แล้ว แค่ไม่เคยพิมพ์ลงเอกสาร) |
| รถส่วนตัว ขาไป | วันที่ **ออกเดินทาง** |
| รถส่วนตัว ขากลับ | วันที่ **กลับถึง** |

ไปกลับวันเดียวกัน สองบรรทัดก็ขึ้นวันเดียวกัน ซึ่งถูกต้องตามจริง

**ขากลับกรอกจุดเริ่มต้นเองได้แล้ว** — เดิมระบบบังคับว่าขากลับต้องออกจากจุดหมายขาไปเสมอ
กรณีจริงอย่าง ที่พัก → โรงแรม แล้ววันต่อมา โรงพยาบาล → ที่พัก จึงเขียนไม่ได้เลย
ไม่ใช่แค่แผนที่ แต่ตัวข้อมูลก็เก็บไม่ได้ ตอนนี้มีช่อง "จุดเริ่มต้นขากลับ" เพิ่มมา
ปล่อยว่าง = ออกจากจุดหมายขาไปเหมือนเดิม แผนที่ในเอกสารวาดขากลับจากจุดนั้นและมีหมุดของตัวเอง

**แก้อีกสามจุดให้ตรงเอกสารต้นแบบ**

- ค่าน้ำมันแยกยอดรายขา ไปที่บรรทัดระยะทางของขานั้น (เดิมรวมเป็นก้อนเดียวคร่อมห้าแถว)
- ตีเส้นครบทุกบรรทัดทั้งสามคอลัมน์ (เดิมเชื่อมช่องเป็นบล็อก)
- ประเภทค่าใช้จ่ายใช้ช่องสี่เหลี่ยม ☐/☑ ส่วนทิศทางใช้วงกลม ◯/◉ ตามแบบฟอร์มจริง
  และเติมข้อความอธิบายใต้ "ค่าใช้จ่ายอื่นๆ (โปรดระบุ)"

**ทดสอบ 40 ข้อ** — วันที่ลงบรรทัดถูกทั้งกรณีคนละวันและวันเดียวกัน · Taxi ใช้วันของรายการตัวเอง ·
ระยะทางและยอดเงินแยกบรรทัดถูกต้อง · ขากลับออกจากจุดที่กรอกเอง · ใบเก่าที่ไม่มีคีย์ใหม่
ยังออกจากจุดหมายขาไปเหมือนเดิม · เอกสารยังพอดีหน้า A4 · ช่องใหม่ในฟอร์มกรอกได้จริง

**สร้างเอกสารจริงทั้ง 76 ใบใหม่: ไม่พังสักใบ** เปลี่ยนหน้าตา 33 ใบที่ออกใบค่าพาหนะ
(อีก 43 ใบออกแต่ใบรายงาน จึงไม่กระทบ) และเทียบข้อความทีละคำแล้ว **ไม่มีเนื้อหาหายไปเลย**
มีแต่ส่วนที่เพิ่มเข้ามาคือวันที่รายขากับข้อความอธิบาย

---

## 2.7.0 — 21 ก.ย. 2569

**ตัวหนังสือในช่องกิจกรรมซ้อนทับกันใน PDF**

ชื่อกิจกรรมยาวๆ ออกมาเป็นข้อความสองชั้นทับกันจนอ่านไม่ออก

ต้นเหตุ: html2canvas วัดความกว้างข้อความรอบหนึ่ง แล้ววาดจริงอีกรอบ
ถ้าฟอนต์เปลี่ยนคั่นกลาง จุดตัดบรรทัดสองรอบไม่ตรงกัน ข้อความชุดเดียวจึงถูกวาดสองแบบทับกัน

ที่ฟอนต์เปลี่ยนกลางคัน เพราะโค้ดรอฟอนต์ไม่ครบ — Google Fonts แบ่งไฟล์ตาม `unicode-range`
เป็นชุดละติน ไทย เวียดนาม แยกกัน และ `document.fonts.load()` ถ้าไม่ระบุข้อความ
จะใช้ข้อความทดสอบภาษาอังกฤษ จึงดึงมาแค่ชุดละติน
ซ้ำร้าย หน้าเว็บทั้งหน้าใช้ IBM Plex Sans Thai ส่วนเอกสารใช้ Sarabun
**ชุดอักษรไทยของ Sarabun จึงไม่เคยถูกโหลดเลย** จนกว่าจะแทรกเอกสารลงหน้า แล้วก็มาไม่ทัน

แก้โดยส่งอักษรไทยเข้าไปในคำสั่งโหลดด้วย เบราว์เซอร์จึงรู้ว่าต้องไปเอาไฟล์ชุดไหน
วัดได้ว่าวิธีเดิมชุดอักษรไทยยังไม่พร้อม ส่วนวิธีใหม่พร้อมครบทั้งตัวปกติและตัวหนา

ช่องที่แบ่งบรรทัดเองไว้แล้ว (ที่อยู่ จาก-ถึง) ไม่เคยซ้อน เพราะไม่ต้องให้ html2canvas ตัดบรรทัดให้
ส่วนช่องกิจกรรมปล่อยให้เบราว์เซอร์ตัด และไม่เคยถูกแก้มาตั้งแต่ต้น

**กรอกค่าที่จอดรถ แต่พิมพ์ออกมาเป็นค่าทางด่วน**

เอกสารมีบรรทัด "ค่าทางด่วน" กับ "ค่าที่จอดรถ" แยกกันอยู่แล้ว
แต่ฟอร์มมีช่องรวม "ค่าทางด่วน/ที่จอดรถ" ช่องเดียว ยอดจึงลงบรรทัดค่าทางด่วนเสมอ
และบรรทัดค่าที่จอดรถว่างเปล่าตลอดกาล

ตอนนี้แยกเป็นสองช่อง — `3.1 ค่าทางด่วน` กับ `3.2 ค่าที่จอดรถ` ยอดลงบรรทัดของตัวเอง
วงกลมหน้าบรรทัดถูกทำเครื่องหมายตามยอดที่กรอก และหน้าสถิติแยกยอดสองก้อนนี้ออกจากกันแล้ว

**เพดานความยาวชื่อกิจกรรม**

วัดจริงแล้วหน้าเอกสารเริ่มล้น A4 (1085px) ที่ราว **250 ตัวอักษร**
ตั้งเพดานไว้ 200 ตัวอักษร ซึ่งยังเหลือที่ว่าง 6px และเผื่อกว่าใบที่ยาวที่สุดที่ใช้จริง (158 ตัวอักษร)
มีตัวนับขึ้นเตือนตั้งแต่ 150 ตัวอักษร ไม่รบกวนตอนพิมพ์สั้นๆ

ใบเก่าที่ยาวเกินอยู่แล้วจะไม่ถูกตัดทิ้ง แต่ตัวนับจะขึ้นสีแดงบอกให้ย่อเมื่อเปิดแก้ไข

**ทดสอบ 21 ข้อ และเทียบเอกสารจริงทั้ง 76 ใบก่อน-หลัง พบว่าเหมือนเดิมทุกตัวอักษรทั้ง 76 ใบ**
การแก้ครั้งนี้จึงไม่กระทบเอกสารที่เคยออกไปแล้วเลย

---

## 2.6.0 — 17 ก.ย. 2026

**ปิดแอปชั่วคราวได้ โดยไม่แตะข้อมูลและเปิดกลับได้ทุกเมื่อ**

ต้องการปิดระบบเพื่อปรับปรุง โดยข้อมูลต้องอยู่ครบและเปิดกลับได้ทันที

**สวิตช์คือไฟล์เปล่าไฟล์เดียว** `maint/ON` — มีไฟล์ = ปิด, ไม่มี = เปิด
nginx อ่านใหม่ทุกคำขอ จึงสลับได้ในวินาทีเดียว **ไม่ต้อง reload ไม่ต้อง restart ไม่ต้อง build**
และไม่แตะฐานข้อมูล ไม่แตะโค้ด ไม่แตะคอนเทนเนอร์

```
./scripts/maintenance.sh on      ปิด
./scripts/maintenance.sh off     เปิด
./scripts/maintenance.sh status  ดูสถานะ
```

**ทำไมด่านอยู่ในคอนเทนเนอร์ ไม่ใช่ที่ nginx ของโฮสต์** — ไล่ดูแล้วพบว่าแอปมีทางเข้าสามทาง
คือ `promberk.ha.or.th`, IP พอร์ต 80 (ผ่าน `motor_pool`) และ `/prompt` บน 443 (ผ่าน `hospitalbilling-ssl`)
สองทางหลังเป็น config ของทีมอื่น ปิดที่โฮสต์จึงต้องแก้ไฟล์ของคนอื่นและยังเสี่ยงปิดไม่สนิท
แต่ทั้งสามทางวิ่งมาลงที่คอนเทนเนอร์นี้ทางเดียว วางด่านตรงนี้จึงปิดครบและไม่แตะของใครเลย

**ทีมงานยังเข้าได้ระหว่างปิด** ผ่านลิงก์ลับที่ตั้งใน `.env` (`MAINT_TOKEN`) เปิดครั้งเดียว
เบราว์เซอร์จำคุกกี้ไว้ 90 วัน ใช้ได้จากทุกเครื่องทุกเน็ต ไม่ผูกกับ IP
ลิงก์ไม่อยู่ใน git เพราะ repo นี้เป็น public และถ้าไม่ตั้งค่าไว้ = ไม่มีใครผ่านด่านได้

**ทดสอบ 71 ข้อ** — ตอนเปิดทุกหน้าเข้าได้เหมือนเดิม · ตอนปิด ปิดครบทั้งหน้าเว็บและ API ทุกเส้นทาง ·
หน้าปิดปรับปรุงขึ้นจริงพร้อมโลโก้และข้อความยืนยันว่าข้อมูลไม่หาย · ลิงก์ลับเข้าได้ ลิงก์เดามั่วเข้าไม่ได้ ·
ยกเลิกสิทธิ์ตัวเองได้ · เปิดกลับแล้วทุกหน้ากลับมา 200 เหมือนเดิมเป๊ะ · สลับปิดเปิดรัว ๆ ห้ารอบยังนิ่ง ·
ฐานข้อมูล integrity ok และจำนวนระเบียนไม่ขยับตลอดการทดสอบ

---

## 2.5.0 — 15 ก.ย. 2026

**จัดลำดับช่องกรอกให้ล้อตามเอกสาร PDF**

เดิมฟอร์มเอาแผนที่กับค่า Taxi ขึ้นก่อนค่าที่พัก และซ่อนค่า Taxi ไว้ในกล่องแผนที่
ทั้งที่ในเอกสารมันคือข้อ 2.1 แยกต่างหาก คนกรอกจึงไล่ตามกระดาษไม่ได้

ตอนนี้เรียงตามเลขข้อในเอกสารตรง ๆ

| | |
|---|---|
| **1. ค่าที่พัก** | ชื่อโรงแรม · รายการที่พัก |
| **2. ค่าพาหนะ** | 2.1 ค่าพาหนะรับจ้าง (Taxi) · 2.2 ค่าน้ำมันรถ (เส้นทางและระยะทาง) · 2.3 ค่าตั๋วเครื่องบิน |
| **3. ค่าใช้จ่ายอื่นๆ** | 3.1 ค่าทางด่วน/ที่จอดรถ · 3.2 ค่าเบี้ยเลี้ยง/อื่นๆ |

**ตรวจก่อนย้ายว่าจะกระทบอะไร** — ไล่หาโค้ดที่ผูกกับการซ้อนกันของกล่อง ไม่ใช่ผูกกับ id
ไม่พบเลยสักจุด (`closest()` ทุกจุดชี้ไปที่แถวย่อยที่ย้ายไปพร้อมกล่อง) และไม่มี CSS ที่อ้างถึงกล่องเหล่านี้
ย้ายทั้งก้อนด้วยการนับแท็ก ไม่พิมพ์ HTML ใหม่เอง ทุกกล่องยังใช้ id เดิม

**ทดสอบ 19 ข้อเฉพาะเรื่องนี้** — ลำดับที่เห็นบนจอเรียงถูกจริง · หัวข้อตรงเลขข้อในเอกสาร ·
แต่ละหมวดโชว์/ซ่อนถูก (ใบรายงานอย่างเดียวไม่มี Taxi และแผนที่, ใบพาหนะอย่างเดียวไม่มีที่พัก) ·
ทุกช่องกรอกได้จริงไม่มีช่องที่ถูกปิด · แผนที่ยังทำงานหลังย้ายที่

**ชุดทดสอบเดิมผ่านครบ 95 ข้อ และเอกสารทั้ง 70 ใบเหมือนเดิมทุกตัวอักษร**
การจัดลำดับไม่กระทบข้อมูลที่บันทึกหรือเอกสารที่ออก

---

## 2.4.0 — 15 ก.ย. 2026

**ตั้งวันที่แยกรายเอกสารได้ เมื่อวันบนสองใบไม่ตรงกัน**

เดิมช่องเดียวคุมวันที่ทั้ง 6 จุดในทุกใบ ซึ่งถูกเกือบทุกครั้ง แต่บางกรณีวันบนใบรายงาน
กับใบค่าพาหนะไม่ตรงกัน แล้วแก้แยกไม่ได้เลย

**ไม่เพิ่มภาระให้คนที่ไม่ต้องใช้** — ปุ่ม "ตั้งวันที่แยกรายเอกสาร" พับเก็บไว้
และโผล่เฉพาะหมวดที่ออกมากกว่าหนึ่งใบ ออกใบเดียวก็ไม่ต้องมีให้เลือก

กางออกมาแล้วทุกช่อง **เดินตามวันที่หลักให้เองจนกว่าจะแตะ** แตะใบไหนใบนั้นหยุดตาม
ใบที่เหลือยังตามต่อ และมีปุ่มให้ทุกใบกลับไปใช้วันที่หลักได้

สร้างช่องเฉพาะเอกสารที่หมวดนั้นออกจริง ไม่โชว์ครบทุกชนิดให้งง
และเก็บลงฐานข้อมูลเฉพาะใบที่ตั้งวันต่างจากวันที่หลัก

**ใบเก่าไม่กระทบ** — ทดสอบเรนเดอร์เอกสารจากใบจริงทั้ง 70 ใบ เหมือนเดิมทุกตัวอักษร

---

## 2.3.9 — 15 ก.ย. 2026

**ลายเซ็นในใบรับรองค่าพาหนะไม่ตกไปอยู่อีกหน้าแล้ว**

วัดทุกหน้าของใบจริง 70 ใบ (179 หน้ากระดาษ) พบล้นแค่ 2 หน้า และล้นแค่ 33px

```
ใบค่าพาหนะสูง      1,118px
หนึ่งหน้า A4 ใส่ได้  1,085px   → เกิน 33px
```

ส่วนลงนามอยู่ท้ายสุดของหน้าจึงถูกดันตกไปทั้งก้อน เกิดกับใบที่ที่อยู่ยาวจนตัดเป็นสองบรรทัด

บีบระยะห่างในบล็อกลงนามลง และเปลี่ยนจากการเว้นบรรทัดด้วย `<br>` มาเป็นกำหนดระยะเป็นตัวเลข
(`<br>` ผูกกับขนาดฟอนต์ คุมความสูงไม่ได้แน่นอน) กับลดขอบในของหน้ากระดาษจาก 10px เหลือ 6px

**ผลตอนนี้: ไม่มีหน้าไหนล้นเลยจาก 179 หน้า** หน้าที่สูงที่สุดเหลือที่ว่าง 22px ราวหนึ่งบรรทัด
เผื่อให้ที่อยู่ที่ยาวกว่านี้ได้อีก

**ข้อความบนเอกสารไม่เปลี่ยนแม้แต่ตัวเดียว** ทั้ง 70 ใบ — เปลี่ยนแค่ระยะห่าง
เพิ่มเครื่องมือตรวจไว้สองตัว: วัดว่าทุกหน้าพอดี A4 และเทียบว่าข้อความไม่เปลี่ยน

---

## 2.3.8 — 14 ก.ย. 2026

**รูปแนบในตัวอย่างเอกสารถูกตัด — แก้แล้ว**

บั๊กที่เพิ่งเกิดใน 2.3.7 เอง ตัวย่อขนาดวัดความสูงเอกสารครั้งเดียวตอนเพิ่งใส่เนื้อหา
ซึ่งตอนนั้นรูปแนบยังไม่มีความสูง พอรูปแสดงผลเสร็จเอกสารสูงขึ้น
แต่กรอบยังค้างที่ความสูงเดิมแล้วตัดส่วนเกินทิ้ง

วัดจริงกับรูปขนาด 1200×1600: **เอกสารสูง 4,849px แต่กรอบค้างที่ 3,190px — หายไป 1,659px**
และไม่ยอมแก้ตัวเองแม้รอถึง 3 วินาที เพราะวัดแค่ครั้งเดียวไม่เคยวัดซ้ำ

ตอนนี้เฝ้าดูขนาดเอกสารด้วย ResizeObserver วัดใหม่ทุกครั้งที่เนื้อหาโตขึ้น
เบราว์เซอร์เก่าที่ไม่มี ResizeObserver ใช้การรอรูปโหลดแทน

**เพิ่มชุดทดสอบไม่ให้กลับมาอีก** — ชุดเดิมจับบั๊กนี้ไม่ได้เพราะแทนรูปด้วยภาพ 1×1
ซึ่งโหลดทันทีจนไม่เกิดปัญหา ตอนนี้สร้างรูปขนาดจริงในการทดสอบด้วย

---

## 2.3.7 — 14 ก.ย. 2026

**ปุ่ม "ตรวจสอบ" ใช้ได้อีกครั้ง และเปลี่ยนเป็นตัวอย่างเอกสารจริง**

กดแล้วขึ้น `Cannot read properties of undefined (reading 'type')` — วัดจากข้อมูลจริงแล้ว
**พัง 66 จาก 70 ใบ (94%)** เปิดได้แต่ใบเก่ายุคแรก 4 ใบ

สาเหตุ: หน้าตรวจสอบอ่าน `c.hotel.type` ตรง ๆ แต่ใบที่บันทึกหลังเปลี่ยนมาใช้ค่าที่พัก
หลายรายการเก็บเป็น `hotelEntries` ไม่มี `c.hotel` ให้อ่าน
(ฝั่งสร้าง PDF เขียนกันไว้ถูกแล้ว ปุ่ม PDF จึงไม่เคยพัง)

**แก้ที่ราก ไม่ใช่เติมเงื่อนไขกันพัง** — เดิมหน้าตรวจสอบประกอบหน้าตาของตัวเองขึ้นมาอีกชุด
แยกจากเอกสารจริง พอฟอร์มเพิ่มช่องใหม่ก็ตามไม่ทัน และพังเมื่อข้อมูลเปลี่ยนรูปแบบ

ตอนนี้แสดง **เอกสารจริงหน้าตาเดียวกับไฟล์ PDF** โดยใช้ตัวสร้างตัวเดียวกัน
ตัวอย่างกับของจริงจึงเพี้ยนจากกันไม่ได้อีก และมีปุ่มดาวน์โหลดอยู่ในกล่องเลย
ดูแล้วพอใจค่อยกด · ลบโค้ดที่ซ้ำซ้อนทิ้ง 190 บรรทัด

**หน้าประวัติเบาลงมาก** — เดิมปุ่มแต่ละแถวฝังข้อมูลทั้งใบไว้ใน HTML
(เฉลี่ยใบละ 303 KB ใบใหญ่สุด 6.7 MB) แอดมินที่เห็น 70 ใบจึงแบกไว้ราว 21 MB
ตอนนี้ส่งแค่เลขที่ใบ วัดได้ 426 KB

และกันบั๊กที่ยังไม่ระเบิด — การ escape เครื่องหมาย `'` ด้วย `'` ใช้ไม่ได้จริงใน HTML
ถ้ามีใครพิมพ์ `'` ลงไปสักตัว ปุ่มทั้งแถวจะพัง ตอนนี้ไม่มีทางเกิดแล้ว

**พิสูจน์ว่าเอกสารไม่เพี้ยน** — เก็บ HTML ของเอกสารจากใบจริงทั้ง 70 ใบไว้ก่อนแก้
แล้วเทียบหลังแก้ **เหมือนเดิมทุกตัวอักษรทั้ง 70 ใบ** และทดสอบตัวฟีเจอร์อีก 17 ข้อ

---

## 2.3.6 — 10 ก.ย. 2026

**บอกให้ชัดว่าวันที่แต่ละช่องไปลงตรงไหนของใบไหน**

ช่อง "วันที่บนเอกสาร" คุมวันที่ในใบรับรองค่าพาหนะด้วย ทั้งช่อง "วัน เดือน ปี"
และวันที่ลงนาม (ตรวจแล้วว่าเป็นค่าตัวเดียวกัน)

พอออกทั้งสองใบพร้อมกัน ฟอร์มเดียวจะมีวันที่ถึงสามชุด ถ้าไม่บอกก็เดาไม่ออกว่าช่องไหนคุมอะไร
จึงเขียนบอกใต้ช่องเลย และเปลี่ยนข้อความตามหมวดที่เลือก

| ช่องในฟอร์ม | ไปพิมพ์ที่ |
|---|---|
| วันที่บนเอกสาร | ใบรายงาน: มุมบนขวา + วันที่ลงนาม · ใบค่าพาหนะ: ช่อง วัน เดือน ปี + วันที่ลงนาม |
| ในระหว่างวันที่ | ใบรายงานเท่านั้น |
| วันออกเดินทาง–กลับถึง | ใบรายงาน บรรทัดวัน-เวลา |

ทดสอบกรณีออกสองใบพร้อมกันโดยตั้งวันที่ทั้งสามชุดให้ต่างกัน แล้วอ่านเอกสารที่ประกอบเสร็จ
ยืนยันว่าแต่ละวันไปลงถูกใบถูกช่อง และไม่มีวันที่กดบันทึกหลุดเข้าไปแม้แต่จุดเดียว

---

## 2.3.5 — 10 ก.ย. 2026

**แก้วันที่บนเอกสารได้แล้ว สำหรับคนที่ทำเรื่องย้อนหลังหรือลงล่วงหน้า**

เดิมวันที่บนเอกสารคือ "นาทีที่กดบันทึก" แก้ไม่ได้เลย ใครทำเรื่องย้อนหลังก็ได้วันผิด
วันที่ตัวนี้ไม่ได้อยู่แค่มุมบนขวา แต่พิมพ์ลงถึง 6 จุดทั่วเอกสาร รวมช่องวันที่ลงนามทุกใบ

เพิ่มช่อง **"วันที่บนเอกสาร"** ไว้ในส่วนที่ 1 — เติมวันนี้ให้อัตโนมัติ ปกติไม่ต้องแตะ
มีวันที่ไทยกำกับใต้ช่อง (เพราะช่องวันที่ของเบราว์เซอร์บางเครื่องขึ้น 09/10/2026
ซึ่งอ่านไม่ออกว่าวันหรือเดือนมาก่อน) และมีปุ่มกดกลับเป็นวันนี้

**เพิ่มช่อง "ในระหว่างวันที่" แยกจากวันเดินทางจริง**

ช่องนี้บนใบรายงานเดิมยึดตามวันออกเดินทาง-กลับถึงเสมอ แต่บางกรณีช่วงที่เขียนในรายงาน
ไม่ตรงกับวันที่เดินทางจริง ตอนนี้แก้แยกได้

**ยังตามวันเดินทางให้อัตโนมัติจนกว่าจะแตะ** แตะแล้วหยุดตาม และมีปุ่มกดให้กลับมาตามได้
คนที่ไม่ต้องใช้จึงไม่ต้องกรอกอะไรเพิ่มเลยแม้แต่ช่องเดียว

**ใบเก่าไม่กระทบ** — ใบที่ไม่มีค่าใหม่ยังใช้วันที่สร้างใบและวันเดินทางเหมือนเดิมทุกประการ
(ทดสอบด้วยการเรนเดอร์เอกสารออกมาเทียบจริง)

---

## 2.3.4 — 10 ก.ย. 2026

**หมวดที่ออกแค่ใบรายงานการเดินทาง ไม่ต้องปักหมุดแผนที่อีกแล้ว**

ช่องกรอกระยะทางมีอยู่บน **ใบรับรองค่าพาหนะ** ใบเดียว — บรรทัด "ระยะทาง ___ ก.ม. x อัตรา"
ส่วนใบรายงานมีแค่บรรทัด 2.2 ค่าน้ำมันรถ ซึ่งเป็นช่องยอดเงิน ไม่มีช่องระยะทางบนกระดาษเลย
(ตรวจด้วยการนับวงเล็บของโค้ดสร้างเอกสาร ไม่ใช่กวาดสายตา)

เดิมเปิดแผนที่ให้ทั้งสองใบ เพราะเข้าใจว่าใบรายงานต้องใช้ระยะทางไปคิดค่าน้ำมัน
ผลคือคนที่เบิกแค่ค่าที่พักต้องมานั่งปักหมุดแผนที่โดยไม่จำเป็น

ตอนนี้แผนที่กับระยะทางขึ้นเฉพาะหมวดที่ติ๊กใบรับรองค่าพาหนะ

**และกันเงินรั่วจากช่องที่ซ่อนอยู่** — ตอนคิดยอดและตอนบันทึก ระยะทางผูกกับใบพาหนะ
ให้ตรงกับที่ซ่อนกล่องไว้ ไม่งั้นสลับหมวดแล้วค่าน้ำมันจากตัวเลขที่ค้างในช่องที่มองไม่เห็น
จะถูกนำมาคิดเป็นเงิน (ทดสอบแล้ว: 800 บาท → 0 บาท เมื่อสลับไปหมวดที่ไม่มีแผนที่)

ตรวจแล้วว่าไม่มีใบเบิกเดิมได้รับผลกระทบ — จาก 65 ใบ ไม่มีใบไหนใช้หมวดที่ออกแค่ใบรายงาน

---

## 2.3.3 — 4 ก.ย. 2026

**ล็อกอิน 365 สำเร็จแล้วแต่ขึ้น "เซสชันหมดอายุ" แล้วเด้งกลับ — แก้แล้ว**

หน้าเว็บส่งหลักฐานยืนยันตัวตนไปสองอย่างพร้อมกัน: token ที่เพิ่งได้จากการล็อกอิน
(ใน header) และคุกกี้ที่ค้างอยู่ในเบราว์เซอร์ แต่ backend เลือกอ่านคุกกี้ก่อนเสมอ

ถ้าคุกกี้นั้นเป็นของเซสชันเก่าที่ถูกลบไปแล้ว ทุกคำขอจะถูกปฏิเสธ 401
ทั้งที่ token ใหม่ถูกต้องทุกประการ ผู้ใช้จึงเห็นเป็น
"ล็อกอิน 365 ผ่าน → ขึ้นเซสชันหมดอายุ → เด้งกลับหน้าล็อกอิน" วนอยู่อย่างนั้น
**และไม่มีทางแก้เองได้** นอกจากล้างคุกกี้ ซึ่งไม่มีอะไรบอกให้ทำ

ตอนนี้ตรวจ token ใน header ก่อน แล้วค่อยตกไปที่คุกกี้ — ใบที่เสียใบหนึ่ง
ไม่บังใบที่ยังใช้ได้อีกต่อไป

**ออกจากระบบลบเซสชันครบทุกใบแล้ว** เดิมลบแค่ใบเดียว อีกใบยังค้างในฐานข้อมูล
และยังใช้เข้าระบบได้

---

## 2.3.2 — 4 ก.ย. 2026

**กดปุ่ม Microsoft 365 แล้วเด้งกลับหน้าเดิม — แก้แล้ว**

เกิดเฉพาะกับคนที่เข้าแอปด้วยเลข IP (`209.15.119.96`) เช่นคนที่ยัง bookmark ของเดิมไว้
การล็อกอินต้องเริ่มที่ `promberk.ha.or.th` เท่านั้น ไม่งั้นคุกกี้กันปลอมแปลง (state)
จะไม่ถูกส่งกลับมาตอน Microsoft redirect

โค้ดเดิมป้องกันถูกแล้ว แต่ทำแค่ส่งผู้ใช้ไปหน้าเข้าสู่ระบบของโดเมนหลักแล้วหยุด
**ซึ่งหน้าตาเหมือนเดิมทุกอย่าง** ผู้ใช้จึงเห็นเป็น "กดแล้วไม่มีอะไรเกิดขึ้น"
และต้องกดซ้ำอีกครั้งโดยไม่มีอะไรบอก

ตอนนี้กดครั้งเดียวไปถึงหน้า Microsoft ได้จากทุกทางเข้า

---

## 2.3.1 — 3 ก.ย. 2026

**ทุกคนแชร์โปรไฟล์ให้เพื่อนร่วมงานใช้ได้ ไม่ใช่แค่ผู้ดูแลระบบ**

เหตุผลเดียวกับรายการตำแหน่ง/สังกัด — ล็อกไว้ที่แอดมินคนเดียวจะเป็นคอขวดโดยไม่จำเป็น
คนที่ทำเรื่องเบิกแทนผู้บริหารหรือทำให้เพื่อนในทีม ควรแชร์ได้เอง

**เก็บชื่อผู้สร้างไว้เสมอ** แม้ตั้งเป็นแบบใช้ร่วมกัน (เดิมตั้ง owner เป็น null)
เพื่อให้คนที่สร้างลบของตัวเองได้ถ้าติ๊กผิด ไม่ต้องรอแอดมิน
ส่วนทะเบียนกลางเก่า 65 รายการที่ไม่มีผู้สร้าง ยังสงวนการลบไว้ให้แอดมินเหมือนเดิม

**แก้ข้อความให้เข้าใจง่าย** — จาก "โปรไฟล์สาธารณะ (Global Profile) — เฉพาะ Admin"
เป็น "ให้เพื่อนร่วมงานใช้โปรไฟล์นี้ได้ด้วย" พร้อมอธิบายว่าใช้ตอนไหน
และบอกว่าเปลี่ยนใจภายหลังได้ · ข้อความตอนลบไม่ได้ก็บอกเหตุผลจริงและทางออก

ทดสอบ 7 เคส: ผู้ใช้ทั่วไปแชร์ได้ · คนอื่นเห็น · เอาติ๊กออกแล้วคนอื่นไม่เห็น ·
เจ้าของลบเองได้ · คนอื่นลบไม่ได้ (403) · ทะเบียนกลางเก่ายังลบไม่ได้ (403)

---

## 2.3.0 — 3 ก.ย. 2026

**สร้างโปรไฟล์ส่วนกลางผ่านหน้าเว็บได้แล้ว**

ระบบออกแบบให้มีโปรไฟล์ 2 แบบ — ส่วนกลางที่ทุกคนเห็น กับส่วนตัวที่เจ้าของเห็นคนเดียว
ฝั่ง backend รองรับครบมาตลอด แต่ฝั่งหน้าเว็บ **ฟีเจอร์นี้ตายสนิท**:
- กล่องติ๊ก "โปรไฟล์สาธารณะ" มีใน HTML แต่ `hidden` ค้างไว้ ไม่มีโค้ดไหนเอาออก
- ค่าในกล่องไม่เคยถูกอ่านตอนบันทึก และไม่เคยถูกส่งไป backend

ผลคือแอดมินสร้างโปรไฟล์ส่วนกลางผ่านหน้าเว็บไม่ได้เลย
(65 รายการที่มีอยู่เกิดจากการแก้ข้อมูลตรงๆ ใน 2.2.0)

ตอนนี้แอดมินเห็นกล่องติ๊ก · ค่าถูกส่งไปจริง · เปิดโปรไฟล์เดิมมาแก้แล้วติ๊กตรงสถานะจริง ·
`PUT /profiles/:id` ยอมให้แอดมินสลับส่วนกลาง ↔ ส่วนตัวได้ (เดิมเปลี่ยนไม่ได้เลย)

ทดสอบ 6 เคส: แอดมินสร้างส่วนกลางได้ · ผู้ใช้ทั่วไปติ๊กก็ถูกลดเป็นส่วนตัว ·
ผู้ใช้เห็นส่วนกลางของแอดมิน · แอดมินสลับกลับได้ · ผู้ใช้สลับเองไม่ได้

---

## 2.2.2 — 3 ก.ย. 2026  ⚠️ แก้ยอดเงินหายในใบเก่า

**เปิดใบเก่ามาแก้ไข แล้วค่าที่พักกับ Taxi หายไปจากยอด**

ใบที่บันทึกด้วยรูปแบบเดิมเก็บที่พักเป็น `costs.hotel = {rate, nights}`
และ Taxi เป็น `taxiOutAmt` / `taxiRetAmt` ก้อนเดียว
ส่วนฟอร์มอ่านแต่รูปแบบใหม่ (`hotelEntries` · `taxiEntries[]`) โดยไม่มีทางถอย

ผลคือเปิดใบเก่ามาแก้ ช่องที่พักกับ Taxi ว่างเปล่า ยอดถูกคิดใหม่โดยไม่นับสองก้อนนั้น
กดบันทึกเมื่อไหร่ ข้อมูลหายและยอดเบิกลดลงเงียบๆ ไม่มีอะไรเตือน

ทดสอบด้วยใบเลียนแบบของจริง: ก่อนแก้ยอดได้ 3,660 หลังแก้ได้ **5,260**
(ที่พัก 1,200 + ค่าน้ำมัน 3,600 + Taxi 400 + ทางด่วน 60) ถูกต้องแล้ว
ออก PDF ของใบเก่าได้ปกติ ไม่มี JavaScript error

**บน production มีใบรูปแบบเก่าอยู่ 4 ใบ** จึงกระทบข้อมูลจริง

---

## 2.2.1 — 1 ก.ย. 2026  ⚠️ แก้ที่ผมสรุปผิดใน 2.1.3

เรนเดอร์เอกสารจริงทั้ง 3 ใบออกมาดู แล้วพบว่าสิ่งที่สรุปไว้ใน 2.1.3 ผิด

**ใบรายงานการเดินทาง (FM-SAM-001-04) ใช้ระยะทางและประเภทรถด้วย**
บรรทัด **2.2 ค่าน้ำมันรถ** คิดจาก ระยะทาง × ประเภทรถ × อัตราค่าน้ำมัน
ตอนตรวจรอบก่อนผมสแกนโค้ดด้วยช่วงบรรทัดคร่าวๆ แล้วพลาดตรงนี้ไป
จึงกันช่องระยะทางไว้ให้ใบพาหนะอย่างเดียว ผลคือหมวดที่ใช้ใบรายงานอย่างเดียว
กรอกค่าน้ำมันไม่ได้เลย และบรรทัด 2.2 พิมพ์ออกมาว่างเสมอ

**ที่อยู่ผู้เดินทางก็อยู่บนกระดาษทั้งสองใบ** แต่ฟอร์มแสดงให้เฉพาะใบพาหนะ
หมวดที่ใช้ใบรายงานอย่างเดียวจึงพิมพ์ที่อยู่ออกมาเป็นเส้นว่างทุกช่อง

ตอนนี้ช่องที่ฟอร์มแสดงตรงกับกระดาษแล้วทุกแบบ ตรวจด้วยการเรนเดอร์จริง:

| หมวด | ที่อยู่ | วัน-เวลา | ระยะทาง | ที่พัก | ตั๋วบิน | Taxi |
|---|---|---|---|---|---|---|
| ใบรายงานอย่างเดียว | แสดง | แสดง | แสดง | แสดง | แสดง | ซ่อน |
| ใบพาหนะอย่างเดียว | แสดง | ซ่อน | แสดง | ซ่อน | ซ่อน | แสดง |
| ทั้งสองใบ | แสดง | แสดง | แสดง | แสดง | แสดง | แสดง |

**บทเรียน:** อ่านโค้ดแล้วเดาว่าเอกสารใช้ฟิลด์ไหน ไม่พอ ต้องเรนเดอร์ออกมาดู

---

## 2.2.0 — 1 ก.ย. 2026

**วงเงินค่าแท็กซี่จำกัดเฉพาะ กทม.และปริมณฑล**
นอกเขตไม่จำกัด เพราะระยะทางต่างจังหวัดต่างกันมาก ตั้งเพดานเดียวไม่ได้
ให้ผู้อนุมัติดูเอง (เขต = กทม. นนทบุรี ปทุมธานี สมุทรปราการ สมุทรสาคร นครปฐม)

ตัดสินจากสองสัญญาณ เพราะข้อมูลที่มีไม่เท่ากันทุกใบ:
- **ชื่อจังหวัด** (`fromAddr.prov` จากโปรไฟล์ · `toProv`) — ถ้ารู้ว่านอกเขต เชื่อทันที
- **พิกัดหมุด** — สัญญาณเดียวที่มีทุกใบ ใช้กรอบพื้นที่คร่าวๆ
  (ช่องจังหวัดปลายทางอยู่บนใบรายงาน ไม่ใช่ใบพาหนะที่มีแท็กซี่ จึงพึ่งอย่างเดียวไม่ได้)

เอนไปทาง "ไม่จำกัด" เมื่อไม่แน่ใจ — ตั้งเพดานผิดที่จะบล็อกใบที่ถูกต้อง
ส่วนการปล่อยผ่านยังมีผู้อนุมัติตรวจอีกชั้น หน้าเว็บบอกเหตุผลด้วยว่าทำไมไม่จำกัด

**โปรไฟล์ 65 รายการที่ผู้ใช้ทั่วไปมองไม่เห็น** (ข้อมูล ไม่ใช่โค้ด)
ทะเบียนกลางถูก migrate มาโดยไม่มีเจ้าของและไม่ได้ตั้งเป็นส่วนกลาง
ผลคือทุกคนยกเว้นแอดมินเห็น 0 รายการ ฟีเจอร์กรอกอัตโนมัติจึงใช้ไม่ได้เลย
ปรับเป็นส่วนกลางแล้ว ผู้ใช้ทั่วไปเห็น 65 รายการ + ของตัวเอง

**หนี้ที่ค้างไว้:** `backend/validation.js` เขียนหัวไฟล์ว่า "ใช้ร่วมกัน Frontend
และ Backend" แต่หน้าเว็บไม่ได้โหลดไฟล์นี้ กฎถูกเขียนซ้ำด้วยมือทั้งสองฝั่ง
เป็นสาเหตุที่บั๊กหลายตัวเกิดฝั่งเดียว ควรทำให้ใช้ไฟล์เดียวกันจริงในอนาคต

---

## 2.1.4 — 1 ก.ย. 2026  ⚠️ แก้ข้อมูลหาย

**กดแก้ไขรายการเก่าแล้วไฟล์แนบหายทั้งหมด**
`PUT /expenses/:id` เขียนทับข้อมูลทั้งก้อน เก็บไว้แค่ `createdAt`
ส่วนหน้าเว็บสร้าง `attachments` ใหม่จากช่องอัปโหลดซึ่งว่างเปล่าเสมอตอนแก้ไข
ผลคือใบเสร็จที่แนบไว้เดิมถูกลบทิ้งทุกครั้งที่กดแก้ไข ทั้งที่ตอนออก PDF
จากหน้าประวัติยังเห็นไฟล์อยู่ครบ

แก้สองชั้น: หน้าเว็บเอาไฟล์เดิมติดไปด้วยและแสดงให้เห็นว่ายังอยู่
(พร้อมปุ่มลบทีละไฟล์ถ้าตั้งใจลบจริง) และ backend กันไว้อีกชั้น —
ถ้า payload ไม่ส่งไฟล์มาเลยทั้งที่ของเดิมมี จะคืนของเดิมให้

**ต้องปักหมุดแผนที่ใหม่ทุกครั้งที่แก้ไข**
คืนค่าเฉพาะหมุดขากลับ ส่วนต้นทาง-ปลายทางไม่ได้คืน ทั้งที่พิกัดเก็บไว้ครบ
ตอนนี้คืนหมุดและเส้นทางให้ครบ ไม่ต้องค้นหาใหม่

**ไม่บังคับแนบไฟล์ซ้ำตอนแก้ไข** ถ้าใบนั้นมีไฟล์แนบอยู่แล้ว

---

## 2.1.3 — 1 ก.ย. 2026  ⚠️ แก้เรื่องยอดเงิน

ไล่ตรวจทั้งระบบหาบั๊กชนิดเดียวกับที่เจอใน 2.1.0 (กฎที่ไม่ได้กันตามชนิดเอกสาร)
แทนที่จะรอให้เจอทีละจุด พบเพิ่ม 4 เรื่อง หนึ่งในนั้นคิดเงินผิด

**1. ยอดเบิกรวมค่าของช่องที่ถูกซ่อนอยู่** ← ร้ายแรงที่สุด
`calculateTravelTotal()` บวกทุกช่องโดยไม่สนว่าช่องนั้นแสดงอยู่ไหม
กรอกค่าที่พักไว้ในหมวดหนึ่ง แล้วสลับไปหมวดที่ใช้ใบรับรองค่าพาหนะอย่างเดียว
ค่าที่พักยังถูกบวกอยู่ ทั้งที่บนกระดาษไม่มีบรรทัดนั้นให้อ้างอิง
ตอนนี้คิดเฉพาะช่องที่แสดงจริง และตอนบันทึกก็เก็บเฉพาะช่องนั้นเหมือนกัน

**2. กฎที่พักฝั่ง backend ไม่เคยทำงานเลย**
อ่าน `costs.hotel` (รูปแบบเก่า) แต่หน้าเว็บส่ง `costs.hotelEntries` มานานแล้ว
ผลคือค่าที่พักไม่เคยถูกนับเข้าเงื่อนไข "ต้องแนบใบเสร็จ"
ตอนนี้อ่านทั้งสองรูปแบบ ใบเก่าในฐานข้อมูลก็ยังตรวจได้

**3. กฎ Taxi และค่าที่พักไม่ได้กันตามชนิดเอกสาร**
รอดมาเพราะฟอร์มซ่อนช่องไว้ — เหตุผลเดียวกับที่เคยคิดว่าระยะทางปลอดภัย
แล้วก็พังจริงมาแล้ว จึงกันให้ชัดทั้งหมด

**4. ค่าตั๋วเครื่องบินค้างข้ามหมวด** กันเพิ่มเหมือนค่าที่พัก

ทดสอบครอบคลุม 8 เคส รวมใบเก่ารูปแบบเดิม ผ่านทั้งหมด

---

## 2.1.2 — 1 ก.ย. 2026

**ตำแหน่ง / สังกัด: เพิ่มแล้วเข้ารายการทันที**

ระบบออกแบบให้ใครก็เพิ่มค่าใหม่ได้ (เพราะยังไม่รู้ว่าจะมีตำแหน่งอะไรบ้าง)
แต่กด "เพิ่ม …" แล้วมันแค่ใส่ค่าลงช่อง เปิดรายการดูอีกทีก็ยังไม่มี
ค่าจะไปถึงเซิร์ฟเวอร์ก็ต่อเมื่อกดบันทึกทั้งใบ — เหมือนกดแล้วไม่เกิดอะไรขึ้น

ตอนนี้กดปุ๊บเข้ารายการปั๊บ และบันทึกขึ้นเซิร์ฟเวอร์ให้คนอื่นเห็นด้วยทันที
พิมพ์แล้วกด Enter เฉยๆ โดยไม่เลื่อนลงไปที่แถว "เพิ่ม" ก็นับเป็นการเพิ่มเหมือนกัน
ใช้ได้ทั้งหน้าทำรายการเบิกและหน้าโปรไฟล์

ยืนยันแล้วว่า **ผู้ใช้ทั่วไป (role user) เพิ่มได้** ไม่ใช่เฉพาะผู้ดูแล —
`/settings/list-append` ใช้ `requireAuth` และรับได้เฉพาะ `positionList`
กับ `affiliationList` เท่านั้น คีย์อื่นถูกปฏิเสธ

---

## 2.1.1 — 1 ก.ย. 2026

**แผงสถานะระบบ** ในหน้าผู้ดูแล (ตั้งค่าระบบ)

เพราะ "เวอร์ชันตรงกัน" ไม่ได้แปลว่า "ทำงานเหมือนกัน" — ค่าตั้งค่าบางอย่าง
เช่นการเปิดใช้ Microsoft 365 อยู่ในไฟล์ `.env` ของแต่ละเครื่อง ไม่เดินทางไปกับ git
เปิดหน้านี้เทียบกันสองฝั่งได้เลย ถ้าตรงกันทุกบรรทัด = ทำงานเหมือนกันจริง

แสดง: ระบบนี้คือ UAT หรือ PROD · เวอร์ชัน · เวลาที่อัปเดตล่าสุด ·
Microsoft 365 เปิด/ปิด · สมัครสมาชิกเองได้ไหม · ใครเข้าด้วยรหัสผ่านได้บ้าง

---

## 2.1.0 — 1 ก.ย. 2026

**เข้าสู่ระบบด้วย Microsoft 365**
- ล็อกอินด้วยบัญชีองค์กร (Microsoft Entra ID) ปิดการสมัครสมาชิกเอง
- ผู้ใช้ใหม่ถูกสร้างบัญชีอัตโนมัติเมื่อล็อกอินครั้งแรกด้วยอีเมลในองค์กร
- ผู้ดูแลระบบยังเข้าด้วยรหัสผ่านได้ (ซ่อนไว้เป็นทางเข้าสำรอง เผื่อ Entra มีปัญหา)
- ปุ่ม "เข้าด้วยบัญชีอื่น" สำหรับเครื่องที่ใช้ร่วมกัน

**แก้บั๊ก**
- ใบรายงานการเดินทาง (FM-SAM-001-04) ไม่บังคับกรอกระยะทางอีกแล้ว —
  บนกระดาษไม่มีช่องนี้ ระยะทางอยู่ในใบรับรองค่าพาหนะ (FM-SAM-095-00) เท่านั้น
- ไฟล์แนบบังคับตามที่แอดมินตั้งในหมวดหมู่ ไม่ใช่บังคับตายตัวจากโค้ด
- ค่าประเภทรถไม่ค้างข้ามหมวดหมู่อีกแล้ว
- ไฟล์ `.js`/`.css` ตั้ง `Cache-Control` แล้ว — เดิมเบราว์เซอร์เก็บของเก่าไว้
  ทำให้แก้โค้ดแล้ว deploy ไปแต่ผู้ใช้ยังเห็นของเดิมและหาสาเหตุไม่เจอ

**หน้าตา**
- โลโก้แอปในหน้าเข้าสู่ระบบ · หน้าแรก (คู่โลโก้สถาบัน) · แถบบนสุดทุกหน้า
- แถบบนสุดเปลี่ยนจาก `Prompt-Berk` เป็น **พร้อมเบิก**
- หน้า portal และ deploy-info ใช้ฟอนต์ไทย IBM Plex Sans Thai

**ความปลอดภัย**
- ย้ายกุญแจ Longdo API ออกจากโค้ดไปไว้ในไฟล์ `.env` (repo นี้เป็น public)
- `.gitignore` กันไฟล์ใบรับรอง/กุญแจทุกนามสกุล

---

## 2.0.0 — 31 ส.ค. 2026

- เปลี่ยนชื่อเป็น **พร้อมเบิก / Promberk**
- URL หลัก `https://promberk.ha.or.th` พร้อมใบรับรอง SSL จริง (ไม่ขึ้นแดง)
- `https://209.15.119.96/` ชี้มาที่พร้อมเบิก · Hospital Billing ย้ายไป `/hospitalbill`
- แก้วงเงิน Taxi ให้ฟังค่าที่แอดมินตั้ง (เดิมฝัง 300 ไว้ในโค้ด)
- แก้หมวดหมู่ที่ใช้ใบรายงานอย่างเดียวแล้วฟอร์มไม่ขึ้นช่องให้กรอก
- รองรับขากลับที่ไม่ได้กลับจุดเดิม · ค้นหาสถานที่แม่นขึ้น · สมุดสถานที่
- หน้าแรกใหม่ · บีบอัดรูปแนบอัตโนมัติ · แก้ PDF หลายจุด
